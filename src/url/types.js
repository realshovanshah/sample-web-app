/**
 * @typedef {Object} UrlSnapshot
 * @property {String} url
 * @property {String} type
 */

/**
 * @typedef {Object} AppState
 * @property {('initial'|'loading'|'failure'|'success')} status
 * @property {String} message
 */
