// Note: This is just a sample, for now.

// import {requestUrlSnapshot} from '../src/url/service';

// describe('requestUrlSnapshot', () => {
//   const validUrl = 'meetup.com';

//   beforeAll(() => {
//     // init mocks
//   });

//   it('should return a UrlSnapshot when the url is known', async () => {
//     const result = await requestUrlSnapshot(validUrl);
//     expect(result).toHaveProperty('url');
//     expect(result).toHaveProperty('type');
//   });


//   it('should return null when the url is unknown', async () => {
//     const result = await requestUrlSnapshot('https://www.ecosia.com');
//     expect(result).toBeNull();
//   });
// });
